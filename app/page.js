export default function Home() {
  return (
    <main style={{ padding: 32, maxWidth: 960, margin: '0 auto' }}>
      <h1 style={{ margin: 0 }}>Vertolio</h1>
      <p style={{ marginTop: 12 }}>
        Next.js + Sanity CMS + Vercel. This repo is now non-empty so Vercel can import it.
      </p>
      <p style={{ opacity: 0.75 }}>
        Next: connect this repo to Vercel, then we’ll add the Sanity schemas for portfolio + blog and wire the pages.
      </p>
    </main>
  );
}
